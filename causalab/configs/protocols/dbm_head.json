{
  "header": {
    "protocol_version": "3",
    "description": "Attention-head DBM: a plain swap through a gate featurizer grouped by head. The gate sits on attention_premix - the o-projection's input, head-major - and holds ONE theta per head, expanded over that head's coordinates, so the mask it fits selects heads: sigma(theta_h)*x_src + (1-sigma(theta_h))*x_base on every coordinate of head h. Group map (heads, head_dim) derived from (model, site) and stamped into the bundle; the l1 term is the mean soft mask over heads, so the penalty is the selected-head count, and fit_diagnostics.json's hard_mask_size counts heads. Layer 19 is a full-attention layer of Qwen3.6-35B-A3B (layer_types repeat 3 DeltaNet + 1 full attention); on a Gated DeltaNet layer set sites.target.component to delta_premix - the out-projection's input, value-head space - and the same document masks value heads. Flat rather than an application of methods/dbm.json because the gate unit, the component and the penalty's meaning all differ from the per-coordinate residual-stream fit. train.eval.split is the held-out validation split; iia.json is the TRAIN score - read train_eval.json for the validation one and dbm_head_apply.json for a confirmation replay. As with dbm.json, gate on decisive_fraction before believing a number: the l1 weight below is a starting point, not a tuned one."
  },
  "model": {"key": "Qwen/Qwen3.6-35B-A3B", "revision": "main", "dtype": "bf16"},
  "data": {
    "base": {"dataset": "natural_domains_arithmetic/data/weekdays#train", "field": "input"},
    "counterfactual": {
      "dataset": "natural_domains_arithmetic/data/weekdays#train",
      "field": "counterfactual_inputs[0]"
    }
  },
  "method": {
    "sites": {
      "target": {"component": "attention_premix", "layers": [19]},
      "lm_head": {"component": "lm_head"}
    },
    "featurizers": {"gate": {"kind": "gate", "group": "head"}},
    "reads": {
      "v_cf": {
        "site": "target",
        "pos": -1,
        "model": "original",
        "input": "counterfactual",
        "featurizer": "gate"
      },
      "logits": {"site": "lm_head", "pos": -1, "model": "masked", "input": "base"}
    },
    "writes": {
      "mask": {"site": "target", "pos": -1, "featurizer": "gate", "do": {"swap": "v_cf"}}
    },
    "intervened_models": {
      "masked": {"input": "base", "writes": ["mask"]}
    },
    "metrics": {
      "iia": {
        "kind": "logit_diff",
        "of": "logits",
        "a": "cf_answer",
        "b": "base_answer",
        "token_form": "space_prefixed"
      },
      "ce": {
        "kind": "cross_entropy",
        "of": "logits",
        "target": "label",
        "token_form": "space_prefixed"
      }
    },
    "train": {
      "objective": [
        [1.0, "ce"],
        [0.01, {"l1": "gate"}]
      ],
      "params": ["gate"],
      "optimizer": {"name": "adamw", "lr": 0.01, "weight_decay": 0.0},
      "steps": {"epochs": 20},
      "batch": {"pairs": 16},
      "anneal": {"gate.theta.temperature": [1.0, 0.01, 0.5]},
      "precision": {"feature": "fp32", "loss": "fp32"},
      "eval": {
        "every": {"epochs": 1},
        "split": "natural_domains_arithmetic/data/weekdays#test",
        "metrics": ["iia"]
      },
      "seed": 0
    },
    "save": [
      {"value": "iia", "model": "masked", "input": "base", "file_path": "iia.json"},
      {"value": "ce", "model": "masked", "input": "base", "file_path": "ce.json"},
      {"value": "gate", "site": "target", "file_path": "gate.safetensors"}
    ]
  }
}
